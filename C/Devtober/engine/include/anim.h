#ifndef _light_engine_anim

    #define _light_engine_anim
    #include "types.h"
    #include "img.h"

    le_img_t * le_load_images(le_str prefixed_path, i32 frames, i32 w, i32 h);

#endif
