rule CC
    command = gcc ${in} -c -o ${out} -I./build/m1/include ${INCLUDES} ${flags}
    description = compiling

rule LINK
    command = gcc ${in} ${flags} ${INCLUDES} -I./build/m1/include -L/Users/mali/homebrew/lib ${libs} -o ${out}
    description = linking 
    
rule AR
    command = ar -rc ${out} ${in}
    description = Linking again

OUT = build/m1/tsukuyomi.out
OBJ_PATH = build/m1/obj/

# Add dyld here
# ninja
#export DYLD_LIBRARY_PATH=build/unix
# ./build/unix/Game
